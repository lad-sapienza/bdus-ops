#!/usr/bin/env bash
# bdus stop <instance|all>   — docker compose stop
set -euo pipefail
. "$BDUS_OPS_DIR/lib/common.sh"
while read -r i; do info "stop $i"; dc "$i" stop; done < <(resolve_instances "${1:-all}")
